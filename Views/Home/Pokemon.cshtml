@model Pokedex.Web.Models.Pokemon

<h1>@Model.Name</h1>

<section id="pokedex-data">
    <p>National Dex:</p>
    <p>@Model.Number</p>

    <p>Type:</p>
    @foreach (var type in Model.Types)
    {
        <p>@type.Name</p>
    }

    <p>Species:</p>
    <p>@Model.Species</p>
</section>